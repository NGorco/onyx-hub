- onyx_type: config
  onyx_id: github-repo-plugin-config
  onyx_data:
    title: GitHub repo list
    backend_entry_file: service.ts
    backend_entry_point: main
    variables:
    secrets_from_env:
      auth_token: AUTH_TOKEN
      org_id: GITHUB_REPO_ORG_ID